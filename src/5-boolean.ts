(()=>{
    let isEnable = true;
    // isEnable = 'as';
    // isEnable = 1212
    isEnable = false;

    let isNew: boolean = false;
    console.log('isNew: ', isNew);
    isNew = true;
    console.log('isNew : ', isNew);

    const random = Math.random();
    console.log('random : ', random);
})